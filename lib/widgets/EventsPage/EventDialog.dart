import 'package:flutter/material.dart';

Dialog eventDialog(BuildContext context) {
  final double w = MediaQuery.of(context).size.width;
  final double h = MediaQuery.of(context).size.height;
  return Dialog(
    child: Text("Hi"),
  );
}
